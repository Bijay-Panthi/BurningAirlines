<%= form_for(flight) do |f| %>
  <% if flight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(flight.errors.count, "error") %> prohibited this flight from being saved:</h2>

      <ul>
      <% flight.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fligh_number %>
    <%= f.number_field :fligh_number %>
  </div>

  <div class="field">
    <%= f.label :origin %>
    <%= f.text_area :origin %>
  </div>

  <div class="field">
    <%= f.label :destination %>
    <%= f.text_area :destination %>
  </div>

  <div class="field">
    <%= f.label :date %>
    <%= f.number_field :date %>
  </div>

  <div class="field">
    <%= f.label :airplane_id %>
    <%= f.number_field :airplane_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
